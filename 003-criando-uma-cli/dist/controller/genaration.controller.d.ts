import { IAnswers } from "interface/answers.interface";
declare class GenerateController {
    gen(answers: IAnswers): void;
    private _execPath;
}
export declare const GenFile: GenerateController;
export {};
